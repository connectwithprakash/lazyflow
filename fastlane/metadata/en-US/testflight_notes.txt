What's New in v1.10.0:

• Quick Capture — tap the pencil button, jot a note, let AI extract tasks. Drag the button to reposition it.
• Focus Mode — Pomodoro timer with configurable intervals, subtasks panel, session persistence across restarts
• Calendar Sync — two-way sync, scheduled times, recurring event support
• Accessibility — labels and hints on all screens, Dynamic Type support

Please test:
- Quick Capture: tap FAB, write a note, verify AI extracts tasks correctly
- Drag FAB: drag the pencil button along the right edge, restart app, verify position persists
- Focus Mode: start a Pomodoro session, pause/resume, verify timer accuracy
- Calendar: create a task with scheduled time, verify it appears in calendar and vice versa

Report issues at https://github.com/connectwithprakash/lazyflow/issues
